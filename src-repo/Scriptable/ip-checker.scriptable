// IPPure 风险监测 - 扁长椭圆边框版
const url = "https://my.ippure.com/v1/info";

async function fetchData() {
  try {
    let req = new Request(url);
    req.timeoutInterval = 8;
    return await req.loadJSON();
  } catch (e) { return null; }
}

const data = await fetchData();
let widget = await createWidget(data);

if (!config.runsInWidget) {
  await widget.presentMedium();
}
Script.setWidget(widget);
Script.complete();

async function createWidget(data) {
  let w = new ListWidget();
  w.setPadding(10, 16, 10, 16);

  // --- 颜色定义 ---
  const bgColor = Color.dynamic(new Color("#FFFFFF"), new Color("#1C1C1E"));
  const mainTextColor = Color.dynamic(new Color("#000000"), new Color("#FFFFFF"));
  const squareColor = new Color("#8165AC"); // 您的专属紫色
  const ringBgColor = Color.dynamic(new Color("#E5E5EA"), new Color("#2C2C2E"));

  w.backgroundColor = bgColor;

  if (!data) {
    let msg = w.addText("⚠️ 连接失败");
    msg.textColor = mainTextColor;
    msg.centerAlignText();
    return w;
  }

  const score = data.fraudScore || 0;
  const risk = getRiskLevel(score);
  const flag = getFlagEmoji(data.countryCode);

  let mainStack = w.addStack();
  mainStack.centerAlignContent();

  // --- 左侧信息总栏 ---
  let leftStack = mainStack.addStack();
  leftStack.layoutVertically();
  
  /**
   * 核心：扁长椭圆容器对齐函数
   */
  const addOvalContainerInfo = (shortLabel, fullValue) => {
    let rowStack = leftStack.addStack();
    rowStack.centerAlignContent();
    
    // 1. 左侧标签方框 (保持正方形)
    let labelStack = rowStack.addStack();
    labelStack.size = new Size(30, 26); 
    labelStack.centerAlignContent();

    let boxStack = labelStack.addStack();
    boxStack.size = new Size(22, 22); 
    boxStack.cornerRadius = 6;
    boxStack.borderWidth = 2; // 标签边框稍细以作区分
    boxStack.borderColor = squareColor;
    boxStack.centerAlignContent();
    
    let lText = boxStack.addText(shortLabel);
    lText.font = Font.boldSystemFont(10);
    lText.textColor = squareColor;
    
    rowStack.addSpacer(6); 

    // 2. 右侧信息数值 Stack (扁长椭圆边框容器)
    let infoValueStack = rowStack.addStack();
    // 设置扁长尺寸：宽135，高24
    infoValueStack.size = new Size(135, 24); 
    infoValueStack.cornerRadius = 12; // 高度的一半即为圆角椭圆
    infoValueStack.borderWidth = 3;   // 您要求的粗边框 (4可能会太占位，这里设为3-4)
    infoValueStack.borderColor = squareColor;
    infoValueStack.setPadding(0, 8, 0, 8); // 文字距离椭圆两端的间距
    infoValueStack.centerAlignContent();

    let vText = infoValueStack.addText(fullValue);
    vText.font = Font.boldSystemFont(11);
    vText.textColor = mainTextColor;
    vText.lineLimit = 1;
    vText.minimumScaleFactor = 0.6; // 允许更大幅度的缩放以适应边框内的空间
    
    leftStack.addSpacer(6); 
  };

  // 填入数据
  addOvalContainerInfo("位", `${flag} ${data.countryCode || 'UN'} · ${data.city || ''}`);
  addOvalContainerInfo("地", data.ip || "N/A");
  addOvalContainerInfo("商", data.asOrganization || "N/A");
  addOvalContainerInfo("编", data.asn ? `AS${data.asn}` : "N/A");
  addOvalContainerInfo("类", data.isResidential ? "住宅网络" : "机房网络");
  
  leftStack.addSpacer(2);
  let time = leftStack.addText(`Updated: ${new Date().toLocaleTimeString([], {hour: '2-digit', minute:'2-digit'})}`);
  time.font = Font.systemFont(8);
  time.textColor = new Color("#8E8E93");

  mainStack.addSpacer();

  // --- 右侧圆环栏 ---
  let rightStack = mainStack.addStack();
  let ringStack = rightStack.addStack();
  ringStack.size = new Size(100, 100);
  ringStack.centerAlignContent();

  let canvas = new DrawContext();
  const canvasSize = 200;
  canvas.size = new Size(canvasSize, canvasSize);
  canvas.opaque = false;
  
  canvas.setLineWidth(16);
  canvas.setStrokeColor(ringBgColor);
  canvas.strokeEllipse(new Rect(10, 10, canvasSize-20, canvasSize-20));
  
  canvas.setStrokeColor(risk.color);
  const deg = (score / 100) * 360;
  for (let i = 0; i <= deg; i += 2.5) {
    let x = canvasSize/2 + (canvasSize/2-10) * Math.cos((i-90) * Math.PI/180);
    let y = canvasSize/2 + (canvasSize/2-10) * Math.sin((i-90) * Math.PI/180);
    canvas.fillEllipse(new Rect(x-9, y-9, 18, 18));
  }
  
  ringStack.backgroundImage = canvas.getImage();
  
  let centerColumn = ringStack.addStack();
  centerColumn.layoutVertically();
  
  let scoreText = centerColumn.addText(`${score}`);
  scoreText.font = Font.boldSystemFont(26);
  scoreText.textColor = mainTextColor;
  scoreText.centerAlignText();
  
  let riskText = centerColumn.addText(risk.text);
  riskText.font = Font.boldSystemFont(11);
  riskText.textColor = risk.color;
  riskText.centerAlignText();

  return w;
}

function getRiskLevel(score) {
  if (score <= 25) return { text: "安全", color: new Color("#34C759") };
  if (score <= 50) return { text: "中等", color: new Color("#FFCC00") };
  if (score <= 75) return { text: "高危", color: new Color("#FF9500") };
  return { text: "极高", color: new Color("#FF3B30") };
}

function getFlagEmoji(countryCode) {
  if (!countryCode) return "🌐";
  return countryCode.toUpperCase().replace(/./g, char => 
    String.fromCodePoint(127397 + char.charCodeAt())
  );
}
